pipeline{
  agent any
  tools {
    maven 'maven'
  }
  stages {
    stage ("initialize") {
      steps {
        sh ''' 
                echo "PATH = ${PATH}
                echo "M2_HOME = ${M2_HOME}
           '''
      }
    }
    stage("build"){
      steps{
        sh "mvn -f /var/lib/jenkins/workspace/webapp/webapp-master/pom.xml clean package "
      }
    }
    stage ("Deploy"){
      steps{
        sshagent(['ubuntu (prod)']){
          sh "scp -o StrictHostKeyChecking=no /var/lib/jenkins/workspace/webapp/webapp-master/target/*.war ubuntu@ip-172-31-14-104:/home/ubuntu/prod/apache-tomcat-9.0.84/webapps/webapp.war"
          
        }
      }
    }
  }
}
